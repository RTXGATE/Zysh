#pragma once

#include <vector>
#include <string>
#include "Token.hpp"

class Lexer
{
private:

	const char* m_input;

	size_t m_position;

public:
	std::vector<Token> Tokenize(const std::string& input);
};
